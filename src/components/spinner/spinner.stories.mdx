import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { Spinner } from './spinner';
import { Flex, Text } from '..';

<Meta title="Components/Spinner" component={Spinner} />

# Spinner

Spinning SVG loading indicator

<Preview withToolbar>
  <Story name="default">
    <Spinner />
  </Story>
</Preview>

Control size using the `size` prop, which maps to `theme.fontSizes`

<Preview>
  <Story name="sizing">
    <Flex alignItems="center">
      <Spinner size={0} mr={2} />
      <Spinner size={1} mr={2} />
      <Spinner size={2} mr={2} />
      <Spinner size={3} mr={2} />
      <Spinner size={4} mr={2} />
      <Spinner size={5} mr={2} />
      <Spinner size={6} mr={2} />
      <Spinner size={7} mr={2} />
      <Spinner size={8} mr={2} />
      <Spinner size={9} mr={2} />
    </Flex>
  </Story>
</Preview>

## Component Props

<Props of={Spinner} />

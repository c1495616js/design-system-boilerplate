import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { Modal, useModal } from '.';
import { Flex, Text, Button } from '..';

<Meta title="Components/Modal" component={Modal} />

# Modal

<Preview withToolbar>
  <Story name="default">
    {() => {
      const { toggle, setTitle } = useModal();
      return (
        <Box>
          <Button
            variant="primary"
            mr={3}
            onClick={() => {
              toggle();
              setTitle('This is Title');
            }}
          >
            Toggle Modal
          </Button>
          <Modal>Body</Modal>
        </Box>
      );
    }}
  </Story>
</Preview>

## Component Props

<Props of={Modal} />

import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { Toast, useToastControls } from '.';
import { Flex, Text, Button } from '..';

<Meta title="Components/Toast" component={Toast} />

# Toast

Spinning SVG loading indicator

<Preview withToolbar>
  <Story name="default">
    {() => {
      const { show, close, closeAll } = useToastControls();
      return (
        <Box>
          <Button variant="primary" mr={3} onClick={() => show('toast-first')}>
            Show First Toast
          </Button>
          <Button variant="secondary" onClick={() => show('toast-second')}>
            Show Seconde Toast
          </Button>
          <Toast uniqueId="toast-first" className="toast-blue">
            First Toast
          </Toast>
          <Toast uniqueId="toast-second" className="toast-blue">
            Second Toast
          </Toast>
        </Box>
      );
    }}
  </Story>
</Preview>

## Component Props

<Props of={Toast} />

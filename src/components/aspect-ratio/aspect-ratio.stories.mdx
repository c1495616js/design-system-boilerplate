import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { AspectRatio } from './aspect-ratio';
import { Heading } from '..';

<Meta title="Components/AspectRatio" component={AspectRatio} />

# AspectRatio

Extension of the `Box` component that maintains content within a desired ratio.

<Preview withToolbar>
  <Story name="AspectRatio">
    <AspectRatio
      ratio={16 / 9}
      bg="brand.primary"
      mb={2}
      p={3}
      display="flex"
      alignItems="center"
      justifyContent="center"
    >
      <Heading variant="heading-1" color="text.inverse">
        Aspect Ratio 16 / 9
      </Heading>
    </AspectRatio>
    <AspectRatio
      ratio={1 / 1}
      bg="brand.primary"
      p={3}
      display="flex"
      alignItems="center"
      justifyContent="center"
    >
      <Heading variant="heading-1" color="text.inverse">
        Aspect Ratio 1 / 1
      </Heading>
    </AspectRatio>
  </Story>
</Preview>
